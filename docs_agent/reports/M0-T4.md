# M0-T4 工作报告

- 任务：sirius-bridge NeoForge MDK 工程骨架（仅搭建，无功能）
- 日期：2026-08-18
- 状态：完成（一处模板默认值遗留，后补修复：mod_license）
- 验收：`gradlew build` 通过（M1-A 时对齐 21.1.248 后复验）

## 交付物

- `build.gradle` / `settings.gradle` / `gradle.properties` / wrapper（Gradle 9.2.0，与 Numen 项目一致）
- `src/main/java/io/sirius/bridge/SiriusBridge.java`：`@Mod("sirius_bridge")` 空构造器
- `src/main/templates/META-INF/neoforge.mods.toml`：**模板方式**（构建时 generateModMetadata 展开属性），产物在 jar 的 META-INF 下；resources/META-INF 留空防冲突
- `.gitignore` / `README.md`

## 关键决策与理由

- 版本组合：MC 1.21.1 / NeoForge 21.1.x / **ModDevGradle 2.0.141** / JDK 21（与本地 Numen 源码版本对齐，方便参考）
- mods.toml 用 templates 目录而非静态文件：可注入 gradle.properties 的版本变量，单一事实来源
- gradle-wrapper.jar 入库（Gradle 官方惯例）

## 教训（已修复）

- `gradle.properties` 的 `mod_license=MIT` 是 **MDK 模板默认值**，T4 未审查 → 流进 mods.toml 显示为 MIT，与仓库 Apache-2.0 矛盾。2026-08-18 修正为 Apache-2.0（commit 1f5cf76）
- **验收纪律**：脚手架/模板生成的元数据（license/author/url）必须逐字段核对，不能只看 build 通过

## 验证方式

gradlew build BUILD SUCCESSFUL；产物 `build/libs/sirius_bridge-0.1.0.jar`

## 交接须知

- 下一步扩展点：M1-B 起在 io.sirius.bridge 包加类；版本变量统一改 gradle.properties
- 已知限制：`apache-23.pom` 的 XML Fatal Error 警告（gradle 缓存清单加载，非致命，未处理）
- 关联报告：M1-A（版本对齐+部署）、M1-B（在此骨架上起服务）
