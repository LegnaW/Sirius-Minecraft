# M3.6 进行中锚点（防中断交接）

状态：**全部完成**。pytest 311/0、gradlew smoke 350/0、真机 3/3 PASS。
最终报告见 `docs_agent/reports/M3.6.md`（本文件保留作过程锚点）。

## 已完成改动

### T1 幻觉防护（brain）
- `sirius-brain/sirius_brain/agent/loop.py`：系统提示新增"观察纪律（防幻觉直答）"节
  （位于安全约束之前）——凡答案取决于当前世界状态的问题必先调感知工具；闲聊/
  知识问答/引用已有工具结果的汇报豁免。原语清单同步 digBlock 掉落说明 + pickup 条目。
- `tests/test_agent_loop.py`：提示契约断言同步（观察纪律 7 项 + pickup 名称）。

### T2 hello_ack 建模（brain）
- `sirius-brain/sirius_brain/protocol/frames.py`：新增 `HelloAckFrame`
  （{type:"hello_ack",ok,protocol_version}，对照真机 Json.helloAck）；`protocol/__init__.py` 导出。
- `sirius-brain/sirius_brain/bridge/client.py`：`_handle_raw` 加 hello_ack 分支
  （`_handle_hello_ack` 模型校验 + debug 日志，不再落"忽略无法识别的帧"）；
  `_wait_hello_ack` 对 hello_ack 记 ok/版本 detail。schema 导出不覆盖握手帧（未硬塞）。
- 测试：test_frames roundtrip+wire 防漂移（线上原文直过模）、test_bridge_client
  hello_ack 识别 + 无"无法识别"日志。

### T3 经验掉落（bridge）
- `DigContracts.java`：`DROPS_WAIT_TICKS=20`（1s）、`DROPS_SCAN_RADIUS=4.0`；
  `aggregateDrops(facts, seenBefore, cx,cy,cz, radius)` 纯聚合（新出现 item 实体按
  注册名累加 count）；`digResult` 加 drops 参数（null 省略字段，旧形态不变）。
- `DigTools.java`：startDig 挖前快照 `itemUuidsNear`（4 格内 item uuid 集，存
  ActiveDig.dropsSeenBefore）；broken 且预算余量 >1s 时 `dropsAfterBreak`：sleep 20
  tick → 主线程 `itemFactsNear`（同 world.query 实体读取路径）→ aggregateDrops 附进
  响应；audit 行加 drops=N。失败/中断一律省略字段（brain 回落旧匹配）。
- `SmokeMain.java`：+5 checks（聚合/diff/形态/常量上限）→ smoke 350/0。

### T3 经验掉落 + pickup 工具（brain）
- `primitives.py`：`_StepResult.drops`；`_extract_drops`（None=旧 jar 回落，[]=真无
  掉落）；dig broken 话术附"掉落 …×n/无掉落"；`_collect_drops(drops=)` 优先 dig 实测
  匹配集；`pickup(item_ids=None)` 缺省捡全部（`_sweep_drops` 支持 wanted=None）。
- `tools.py`：`PICKUP_TOOL="pickup"` 注册（PickupParams：item_ids 1..8 可空、radius
  1..32 默认 12；描述含 Numen 契约+多人服礼仪）；注册表 14→15。
- `mock/fakeworld.py`：fake dig broken 附 drops（挖前快照 diff，与真 bridge 同契约）。
- 测试：+9 → pytest 311/0（TestDigDrops 4 项含"模组方块掉不同名物品"优选验证、
  pickup 缺省/注册表端到端、hello_ack 2 项、frames 1 项、旧 jar 回归）。

## 待办

（无——真机验证与交接报告均已完成；游戏 PID 25736 / 8765 保持运行供复验，退出后可删 %TEMP%\m36\game_stdout.log）
