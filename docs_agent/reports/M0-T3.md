# M0-T3 工作报告

- 任务：协议 JSON Schema 导出（冻结）+ NEKO 兼容帧映射说明
- 日期：2026-08-18
- 状态：完成
- 验收：pytest 全绿（162 项）；schema 产物 27 个自包含可被 Java 消费（M1-B 实证）

## 交付物

- `sirius_brain/protocol/export_schema.py`：导出 CLI（`python -m sirius_brain.protocol.export_schema [--output DIR]`）
- `schema/`：index.json（清单+协议版本 1.0）+ frames/ 9 + tools/ 12 + tasks/ 5 = 27 个文件
- `tests/test_schema_export.py`：102 项（合法性/自包含/正反例/枚举完整/**仓库 schema 与代码重导出同步性**）
- `docs_agent/protocol-neko-mapping.md`：NEKO game_agent 帧双向映射（中文）

## 关键决策与理由

- **自包含单文件**（$ref 全为同文件片段）：Java 主流校验库（networknt/everit）单文件加载零装配——比跨文件 $id 引用稳
- pydantic 2.13 不输出顶层 $schema → 导出时显式注入 draft 2020-12
- 同步性测试：改模型忘重导出 → CI 红。"冻结"的工程保障
- NEKO 映射**以 N.E.K.O 源码为准**（client.py 实际帧集比设计文档多 6 种：alert/inventory/bot_status_nl/ingame_chat/agent_status/query_inventory，均已映射）

## 实现要点

- Java 侧消费入口：仓库 `sirius-brain/schema/`（M1-B 的 syncToolSchemas 任务在构建时单向同步进 jar）
- 五态消费侧证据（写进映射文档）：interrupted/superseded 中性、timeout 干净结局、失败绝不报 ok

## 验证方式

pytest 162 绿；M1-B 进程内互通 9/9 + 真机能力协商实证 schema 正确

## 交接须知

- 下一步扩展点：改协议 → 改 pydantic 模型 → 跑导出 CLI → 同步性测试逼你提交新 schema
- 已知限制：schema 只描述帧结构，不描述时序（时序语义在 sirius-technical.md §8.2 + NEKO 映射文档）
- 关联报告：M1-B（jar 内嵌 schema）、M1-C（工具返回结构以 tools/*.json 为准）
